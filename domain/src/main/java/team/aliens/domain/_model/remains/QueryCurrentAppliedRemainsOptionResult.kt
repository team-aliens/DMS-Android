package team.aliens.domain._model.remains

/**
 * A response returned when querying current applied remains option
 * @property title the title of the remains option
 */
data class QueryCurrentAppliedRemainsOptionResult(
    val title: String,
)
