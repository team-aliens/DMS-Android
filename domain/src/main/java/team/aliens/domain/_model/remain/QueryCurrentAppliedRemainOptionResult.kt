package team.aliens.domain._model.remain

/**
 * @author junsuPark
 * A response returned when querying current applied remain option
 * @property title the title of the remain option
 */
data class QueryCurrentAppliedRemainOptionResult(
    val title: String,
)
