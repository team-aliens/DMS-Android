package team.aliens.dms.android.domain.model._common

/**
 * An enum class of order type
 * @property NEW order by date recently asc
 * @property OLD order by date recently desc
 */
enum class Order {
    NEW, OLD,
    ;
}
